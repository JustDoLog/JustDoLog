# Django
DJANGO_SETTINGS_MODULE=config.settings.local
DJANGO_SECRET_KEY=django-insecure-change-this-in-production

# Debug
DEBUG=True

# Hosts
ALLOWED_HOSTS=localhost,127.0.0.1

# Database - Using SQLite3 in development
# No database environment variables needed for SQLite3

# Email - Using console backend in development
# No email environment variables needed for console backend

# MinIO
MINIO_ROOT_USER=justdolog_minio
MINIO_ROOT_PASSWORD=change_this_password
MINIO_BUCKET_NAME=justdolog-media
MINIO_URL=http://minio:9000

# Comments:
# 1. Copy this file to .env and update the values
# 2. Make sure to change DJANGO_SECRET_KEY in production
# 3. In production, set DEBUG=False and update ALLOWED_HOSTS accordingly 